export default function TeacherDashboardPage() {
  // Placeholder — spec (screens-teacher.md): stat row + recently published list. Real data
  // lands with P3; kept intentionally minimal for MVP.
  return (
    <div>
      <h1 className="text-2xl font-bold">Teacher Dashboard</h1>
      <p className="mt-2 text-sm text-muted-foreground">
        Your questions, quizzes, and results will appear here.
      </p>
    </div>
  );
}
